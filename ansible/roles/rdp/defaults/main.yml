---
# RDP Optimizer - Default Configuration

# RDP credentials for GNOME Remote Desktop
# These are the default credentials that will be set for RDP access
# Change these in your inventory or playbook vars to customize
rdp_username: "hyperi"
rdp_password: "hyperi"

# FreeRDP library version to pin (Ubuntu only)
# ubuntu0.3 is broken (heap corruption). Base 3.16.0+dfsg-2 is known-good.
# See ~/RDP-FREERDP-FIX.md for details.
freerdp_pin_version: "3.16.0+dfsg-2ubuntu0.4"
